<footer class="footer">
    <p class="footer__text">
        <% if (typeof aiEnabled !== 'undefined' && aiEnabled) { %>
            In-shorts summarized by <strong>Claude AI</strong> &middot; News via NewsAPI
        <% } else { %>
            News via NewsAPI &middot; <span class="footer__muted">set <code>ANTHROPIC_API_KEY</code> to switch on AI summaries</span>
        <% } %>
    </p>
</footer>
<script src="/js/app.js" defer></script>
